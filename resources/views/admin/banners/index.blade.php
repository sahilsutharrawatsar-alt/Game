@extends('layouts.admin', ['title' => 'Banners'])
@section('content')
<h1 class="text-3xl font-black">Homepage banners</h1>
<form method="POST" action="{{ route('admin.banners.store') }}" class="mt-6 grid gap-3 rounded-3xl border border-white/10 bg-white/[.04] p-4 md:grid-cols-4">@csrf<input name="title" placeholder="Title" class="rounded-xl bg-black/30 px-3 py-2"><input name="subtitle" placeholder="Subtitle" class="rounded-xl bg-black/30 px-3 py-2"><input name="image" placeholder="Image URL" class="rounded-xl bg-black/30 px-3 py-2 md:col-span-2"><input name="cta_label" placeholder="CTA label" class="rounded-xl bg-black/30 px-3 py-2"><input name="cta_url" placeholder="/venues" class="rounded-xl bg-black/30 px-3 py-2"><input name="sort_order" placeholder="Sort" class="rounded-xl bg-black/30 px-3 py-2"><button class="rounded-xl bg-lime-300 font-bold text-slate-950">Publish</button></form>
<div class="mt-6 grid gap-3">@foreach($banners as $banner)<div class="grid gap-4 rounded-3xl border border-white/10 bg-white/[.04] p-4 md:grid-cols-[160px_1fr_auto]"><img src="{{ $banner->image }}" class="h-24 w-full rounded-2xl object-cover"><div><b>{{ $banner->title }}</b><p class="text-sm text-slate-400">{{ $banner->subtitle }}</p></div><form method="POST" action="{{ route('admin.banners.update',$banner) }}">@csrf @method('PUT')<label class="text-sm"><input type="checkbox" name="is_active" value="1" @checked($banner->is_active)> Active</label><button class="ml-2 rounded-xl border border-white/15 px-3 py-2">Save</button></form></div>@endforeach</div>
@endsection
