@extends('layouts.admin', ['title' => 'Slots'])
@section('content')
<h1 class="text-3xl font-black">Slot timing</h1>
<form method="POST" action="{{ route('admin.slots.store') }}" class="mt-6 grid gap-3 rounded border border-white/10 bg-white/[.04] p-4 md:grid-cols-6">@csrf<select name="venue_id" class="rounded bg-black/30 px-3 py-2">@foreach($venues as $venue)<option value="{{ $venue->id }}">{{ $venue->name }}</option>@endforeach</select><select name="sport_id" class="rounded bg-black/30 px-3 py-2">@foreach($sports as $sport)<option value="{{ $sport->id }}">{{ $sport->name }}</option>@endforeach</select><input name="date" type="date" class="rounded bg-black/30 px-3 py-2"><input name="start_time" type="time" class="rounded bg-black/30 px-3 py-2"><input name="end_time" type="time" class="rounded bg-black/30 px-3 py-2"><input name="price" placeholder="Price" class="rounded bg-black/30 px-3 py-2"><button class="rounded bg-lime-400 font-bold text-slate-950 md:col-span-6">Create slot</button></form>
<div class="mt-6 overflow-x-auto rounded-lg border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/[.04] text-slate-400"><tr><th class="p-3">Venue</th><th>Sport</th><th>Time</th><th>Price</th><th>Status</th></tr></thead><tbody>@foreach($slots as $slot)<tr class="border-t border-white/10"><td class="p-3">{{ $slot->venue->name }}</td><td>{{ $slot->sport->name }}</td><td>{{ $slot->starts_at->format('d M h:i A') }}</td><td>₹{{ number_format($slot->price) }}</td><td>{{ $slot->status }}</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $slots->links() }}</div>
@endsection
